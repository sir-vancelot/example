package com.batman.batdroid.domain.notification

/**
 * A notification which can be extended to publish events on the main thread via the [BatmanNotificationPublisher].
 */
open class BatmanNotification