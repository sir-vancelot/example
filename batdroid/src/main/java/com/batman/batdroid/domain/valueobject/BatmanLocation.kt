package com.batman.batdroid.domain.valueobject

/**
 * A [BatmanLocation] is used as a store of value for locations mappable to an X, Y coordinate system.
 */
data class BatmanLocation (val x:Double = 0.0, val y:Double = 0.0)
